<Page
    x:Class="MoviePreview.Views.MyCollectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    DataContext="{Binding MyCollectViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d"
    xmlns:model="using:MoviePreview.Models"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:i="using:Microsoft.Xaml.Interactivity">
    <Grid
        x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- MyCollectPage_TextTitle.Text="我的收藏" -->
        <TextBlock
            x:Uid="MyCollectPage_TextTitle"
            Grid.Row="0"
            Margin="50, 10"
            FontSize="22"/>
        <ProgressBar
                x:Name="LoadToOther"
                IsIndeterminate="True"
                Visibility="Collapsed"
                VerticalAlignment="Top"
                Grid.Row="0"
                Margin="20, 10, 20 ,0"
                Foreground="Blue"/>
        <ListView
            x:Name="CollectionsView"
            ItemsSource="{x:Bind ViewModel.Collections, Mode=OneWay}"
            Grid.Row="1"
            CanDragItems="False"
            Margin="10"
            ItemClick="CollectionsView_ItemClick"
            IsItemClickEnabled="True"
            Padding="20">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:MovieItem">
                    <RelativePanel Margin="10">
                        <Image
                            x:Name="ImageMovie"
                            Height="180"
                            RelativePanel.AlignLeftWithPanel="True"
                            Source="{x:Bind ImageUri, Mode=OneWay}"/>
                        <TextBlock
                            x:Name="TextTitle"
                            RelativePanel.RightOf="ImageMovie"
                            FontSize="21"
                            Text="{x:Bind TitleCn, Mode=OneWay}"
                            Margin="20, 0, 0, 5"/>
                        <TextBlock
                            x:Name="TextDirectors"
                            RelativePanel.RightOf="ImageMovie"
                            RelativePanel.Below="TextTitle"
                            Text="{x:Bind Directors, Mode=OneWay}"
                            Margin="20, 5"/>
                        <TextBlock
                            x:Name="TextDate"
                            RelativePanel.RightOf="ImageMovie"
                            RelativePanel.Below="TextDirectors"
                            Text="{x:Bind Date, Mode=OneWay}"
                            Margin="20, 5"/>
                        <TextBlock
                            x:Name="TextType"
                            RelativePanel.RightOf="ImageMovie"
                            RelativePanel.Below="TextDate"
                            Text="{x:Bind MovieType, Mode=OneWay}"
                            Margin="15, 5"/>
                        <!-- MyCollectPage_TextNoteHelp.Text="备注： " -->
                        <TextBlock
                            x:Uid="MyCollectPage_TextNoteHelp"
                            x:Name="TextNoteHelp"
                            RelativePanel.RightOf="ImageMovie"
                            RelativePanel.Below="TextType"
                            Margin="20, 5, 5, 0"/>
                        <TextBlock
                            x:Name="TextNote"
                            RelativePanel.RightOf="TextNoteHelp"
                            RelativePanel.Below="TextType"
                            Text="{x:Bind Note, Mode=OneWay}"
                            Margin="0, 5"/>
                        <!-- MyCollectPage_ButtonEdit.Content="修改" -->
                        <Button
                            x:Uid="MyCollectPage_ButtonEdit"
                            x:Name="ButtonEdit"
                            Visibility="Collapsed"
                            Margin="10"
                            AllowFocusOnInteraction="True">
                            <Button.Flyout>
                                <Flyout x:Name="EditFlyout">
                                    <StackPanel>
                                        <!-- MyCollectPage_TextNoteEdit.Header="修改备注为：" -->
                                        <TextBox
                                            x:Uid="MyCollectPage_TextNoteEdit"
                                            x:Name="TextNoteEdit"
                                            Margin="10"
                                            MinWidth="200" />
                                        <!-- MyCollectPage_ButtonConfirm.Content="确定" -->
                                        <Button
                                            x:Uid="MyCollectPage_ButtonConfirm"
                                            HorizontalAlignment="Right"
                                            Click="ChangeNoteButton_Click"/>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                        <AppBarButton
                            Icon="Delete"
                            Click="DeleteButton_Click"
                            RelativePanel.AlignRightWithPanel="True"/>
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
